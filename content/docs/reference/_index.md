---
title: "Reference"
description: "Per-feature reference for every part of the HugoKit window — what each control does and when to use it."
# `type` on this page itself so Hugo picks layouts/reference/list.html
# rather than the parent /docs/ layout. `cascade.type` propagates the
# same value to all descendant feature pages.
type: "reference"
cascade:
  type: "reference"
---
