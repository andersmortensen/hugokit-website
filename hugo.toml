baseURL = 'https://hugokit.com/'
languageCode = 'en'
title = 'HugoKit'
timeZone = 'Europe/Copenhagen'
disableKinds = ['taxonomy', 'term', 'section', 'rss']

[params]
  description = 'HugoKit is the friendly Mac app that runs, previews and publishes your Hugo sites. No terminal, no fuss — one click each.'
  # Peger på den DMG som release-flowet lægger i static/downloads/ (se RELEASING.md).
  # Indtil første release er uploadet kan den midlertidigt sættes tilbage til GitHub-releases-URL'en.
  downloadURL = '/downloads/HugoKit.dmg'
  githubURL = 'https://github.com/andersmortensen/hugokit-source'
  feedbackEmail = 'kontakt@andersmortensen.com'
